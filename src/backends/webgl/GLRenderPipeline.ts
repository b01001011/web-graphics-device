import type { RenderPipeline, RenderPipelineDescriptor } from '../../core'

import { GLDevice } from './GLDevice'

export class GLRenderPipeline implements RenderPipeline {
  
  constructor(device: GLDevice, descriptor: RenderPipelineDescriptor) {
  }
}
